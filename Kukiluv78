local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local MarketplaceService = game:GetService("MarketplaceService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- [[ 🛰️ REMOTE CONFIG ]] --
local VehicleRemote = ReplicatedStorage:WaitForChild("RE"):WaitForChild("1NoMoto1rVehicle1s")

-- ======================== [[ SYSTEM ENCODE + JUNK (ระบบส่วนกลาง) ]] ========================
local _JUNK = "83260119948695& \n [[191]] &%69=00"

local function _urlEncode(str)
    local hex = "0123456789ABCDEF"
    local result = ""
    for i = 1, #str do
        local b = string.byte(str, i)
        result = result .. "%" .. string.sub(hex, math.floor(b/16)+1, math.floor(b/16)+1) .. string.sub(hex, (b%16)+1, (b%16)+1)
    end
    return result
end

local function _buildPayload(realID)
    return _JUNK .. _urlEncode(tostring(realID))
end

local _plainID = ""
local IsPlaying = false
local StartTime = 0
local SongDuration = 0
local VisualSound = nil 

local MainSongLabel
local MainDateLabel
local MainTimeLabel

-- ฟังก์ชันเล่นเพลง (ใช้ร่วมกันทั้งสคริปต์ ไม่ต้องเขียน Junk ซ้ำ)
local function PlayMusicByID(musicID)
    if not musicID or musicID == "" then return end
    VehicleRemote:FireServer("PickingScooterMusicText", "140734563826532", true)
    task.wait(0.01)
    VehicleRemote:FireServer("PickingScooterMusicText", _buildPayload(musicID), true)

    if VisualSound then VisualSound:Destroy() end
    VisualSound = Instance.new("Sound", game:GetService("SoundService"))
    VisualSound.SoundId = "rbxassetid://" .. tostring(musicID)
    VisualSound.Volume = 0
    
    task.spawn(function()
        repeat task.wait() until VisualSound.TimeLength > 0
        SongDuration = math.floor(VisualSound.TimeLength)
        StartTime = tick()
        IsPlaying = true
        VisualSound:Play()
        local success, info = pcall(function() return MarketplaceService:GetProductInfo(tonumber(musicID)) end)
        if success and info then
            if MainSongLabel then MainSongLabel:Set("🎵 เพลง: " .. info.Name) end
            if MainDateLabel then MainDateLabel:Set("📅 เมื่อ: " .. info.Created) end
        end
    end)
end

-- ======================== โค้ดเดิม (หมวด 1-2 อยู่เดิมเป๊ะ) ========================
local Window = Rayfield:CreateWindow({
   Name = "GuHon 191eiei | SYNC FIXED (By.Honkuki78)",
   LoadingTitle = "GuHon XD & Honkuki78",
   LoadingSubtitle = "Server-Side Audio Only",
   ConfigurationSaving = { Enabled = false }
})

local CurrentID = ""
local SelectedPlayer = ""

local function CleanID(str)
    if not str or type(str) ~= "string" then return nil end
    local decoded = str
    while decoded:find("%%") do
        pcall(function() decoded = decoded:gsub("%%(%x%x)", function(hex) return string.char(tonumber(hex, 16)) end) end)
    end
    local allNumbers = {}
    for num in decoded:gmatch("%d+") do table.insert(allNumbers, num) end
    if #allNumbers > 0 then
        local rawId = allNumbers[#allNumbers]
        local cleaned = tostring(tonumber(rawId)) 
        if #cleaned >= 6 then return cleaned end
    end
    return nil
end

local function DeepHunt(target)
    if not target then return nil end
    local foundData = nil
    pcall(function()
        local paths = {target.Character, target:FindFirstChild("Backpack"), target:FindFirstChild("PlayerGui")}
        for _, path in pairs(paths) do
            if path then
                for _, obj in pairs(path:GetDescendants()) do
                    local content = ""
                    if obj:IsA("Sound") then content = obj.SoundId
                    elseif obj:IsA("StringValue") or obj:IsA("TextLabel") then
                        content = obj:IsA("StringValue") and obj.Value or obj.Text
                    end
                    local id = CleanID(content)
                    if id then foundData = {ID = id, Source = obj.Name} break end
                end
            end
            if foundData then break end
        end
    end)
    return foundData
end

local function GetClosestPlayer()
    local target, dist = nil, math.huge
    local lp = game.Players.LocalPlayer
    if not (lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")) then return nil end
    for _, p in pairs(game.Players:GetPlayers()) do
        if p ~= lp and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
            local d = (lp.Character.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude
            if d < dist then target, dist = p, d end
        end
    end
    return target
end

-- [[ 📑 หมวดหมู่ที่ 1: Bypass Logger ]] --
local Tab1 = Window:CreateTab("Bypass Logger", 4483362458)
Tab1:CreateSection("Developed By.Honkuki78")
local ResultLabel = Tab1:CreateLabel("🎯 สถานะ: พร้อมเจาะรหัส (คงเดิม)")

Tab1:CreateButton({
   Name = "🛰️ [ ดึงคนใกล้ที่สุด ]",
   Callback = function()
       local target = GetClosestPlayer()
       if target then
           local data = DeepHunt(target)
           if data then 
               CurrentID = data.ID 
               _plainID = data.ID
               ResultLabel:Set("✅ เจาะได้แล้ว! ID: " .. data.ID) 
               setclipboard(data.ID) 
           end
       end
   end
})

Tab1:CreateButton({
   Name = "⚡ [ เจาะไอดีจริง (No Zeros) ]",
   Callback = function()
       if SelectedPlayer == "" then return end
       local data = DeepHunt(game.Players:FindFirstChild(SelectedPlayer))
       if data then 
           CurrentID = data.ID
           _plainID = data.ID
           ResultLabel:Set("✅ เจาะได้แล้ว! ID: " .. data.ID) 
           setclipboard(data.ID) 
       end
   end
})

local Dropdown = Tab1:CreateDropdown({
   Name = "เลือกเป้าหมาย", Options = {}, CurrentOption = "",
   Callback = function(v) SelectedPlayer = v[1] end,
})
local function Refresh()
    local list = {}
    for _, p in pairs(game.Players:GetPlayers()) do if p ~= game.Players.LocalPlayer then table.insert(list, p.Name) end end
    Dropdown:Refresh(list, true)
end
Refresh()
Tab1:CreateButton({Name = "🔄 รีเฟรชรายชื่อ", Callback = Refresh})

-- [[ 📑 หมวดหมู่ที่ 2: Main Broadcast ]] --
local PlayTab = Window:CreateTab("Main Broadcast", 4483362458)
PlayTab:CreateSection("Skateboard Remote Audio")

PlayTab:CreateInput({
   Name = "ไอดีเพลง", PlaceholderText = "ใส่ ID ที่นี่...",
   Callback = function(Text) 
       local cleaned = CleanID(Text) or Text
       _plainID = cleaned
   end,
})

MainSongLabel = PlayTab:CreateLabel("🎵 เพลงที่สเก็ตบอร์ด: -")
MainDateLabel = PlayTab:CreateLabel("📅 อัปโหลดเมื่อ: -")
MainTimeLabel = PlayTab:CreateLabel("⏳ เวลาเล่น: 00:00 / 00:00")
local VisualLabel = PlayTab:CreateLabel("🔊 [ ⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯ ]")

PlayTab:CreateButton({
   Name = "🔥 [ ส่งรีโมทเปิดเพลงเข้าสเก็ตบอร์ด ]",
   Callback = function()
       if _plainID == "" then return end
       PlayMusicByID(_plainID)
   end
})

PlayTab:CreateButton({
   Name = "⏹️ หยุดส่งรีโมท",
   Callback = function()
       VehicleRemote:FireServer("PickingScooterMusicText", "", true)
       if VisualSound then VisualSound:Stop(); VisualSound:Destroy(); VisualSound = nil end
       IsPlaying = false
       MainTimeLabel:Set("⏳ เวลาเล่น: 00:00 / 00:00")
   end
})

-- ======================== หมวดหมู่ที่ 3 (ดึงค่า Junk จากหมวด 2 มาเล่น) ========================
local VaultTab = Window:CreateTab("📀 ตู้เซฟเพลง", 4483362458)
VaultTab:CreateSection("📝 บันทึกเพลง (ดึงระบบ Junk หมวด 2 มาใช้)")

local savedSongs = {}
local selectedSongIndex = nil
local vName, vID = "", "" 

VaultTab:CreateInput({
    Name = "ชื่อเพลง", PlaceholderText = "ตั้งชื่อ...",
    Callback = function(t) vName = t end
})

VaultTab:CreateInput({
    Name = "ID เพลง", PlaceholderText = "ใส่ ID...",
    Callback = function(t) vID = t end
})

local songDropdown = VaultTab:CreateDropdown({
    Name = "เลือกเพลงจากรายการ", Options = {}, CurrentOption = "",
    Callback = function(opt)
        local selectedName = opt[1]
        if selectedName then
            for i, v in ipairs(savedSongs) do
                if v.name == selectedName then selectedSongIndex = i break end
            end
        end
    end
})

VaultTab:CreateButton({
    Name = "💾 เซฟเพลงลงตู้",
    Callback = function()
        if vName == "" or vID == "" then return end
        table.insert(savedSongs, {name = vName, id = vID})
        local options = {}
        for _, v in ipairs(savedSongs) do table.insert(options, v.name) end
        songDropdown:Refresh(options, true)
        Rayfield:Notify({Title = "สำเร็จ", Content = "บันทึกเรียบร้อย", Duration = 2})
    end
})

VaultTab:CreateButton({
    Name = "▶️ เล่นเพลงนี้",
    Callback = function()
        if selectedSongIndex and savedSongs[selectedSongIndex] then
            -- เรียกใช้ฟังก์ชันเดิมของหมวด 2 ทันที (ซิงค์ Junk และ UI หน้าหลักอัตโนมัติ)
            PlayMusicByID(savedSongs[selectedSongIndex].id)
        end
    end
})

VaultTab:CreateButton({
    Name = "❌ ลบเพลง",
    Callback = function()
        if selectedSongIndex then
            table.remove(savedSongs, selectedSongIndex)
            selectedSongIndex = nil
            local options = {}
            for _, v in ipairs(savedSongs) do table.insert(options, v.name) end
            songDropdown:Refresh(options, true)
        end
    end
})

-- ======================== ระบบ RGB & REAL-TIME SYNC ========================
task.spawn(function()
    local bars = {" ", " ", "▂", "▃", "▄", "▅", "▆", "▇", "█"}
    while true do
        pcall(function() game:GetService("CoreGui").Rayfield.Main.Elements.BackgroundColor3 = Color3.fromHSV(tick() % 5 / 5, 1, 1) end)
        if IsPlaying and SongDuration > 0 then
            local elapsed = math.floor(tick() - StartTime)
            if elapsed <= SongDuration then
                local timeStr = string.format("%02d:%02d", math.floor(elapsed/60), elapsed%60) .. " / " .. string.format("%02d:%02d", math.floor(SongDuration/60), SongDuration%60)
                if MainTimeLabel then MainTimeLabel:Set("⏳ เวลาเล่น: " .. timeStr) end
                local v = ""
                for i = 1, 14 do
                    local loudness = (VisualSound and VisualSound.PlaybackLoudness) or 0
                    v = v .. bars[math.clamp(math.floor(loudness / 40) + 1, 1, #bars)]
                end
                VisualLabel:Set("🔊 [ " .. v .. " ]")
            else
                IsPlaying = false
                if MainTimeLabel then MainTimeLabel:Set("⏳ จบเพลง") end
            end
        end
        task.wait(0.05)
    end
end)
